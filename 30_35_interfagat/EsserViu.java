/*
 * Interfície amb tres mètodes esViu(), mor() i reviu() que permet que altres
 * classes la facin servir. Aquesta interfície es fa servir en qualsevol gat.
 */
 
interface EsserViu {
    // Mètodes de l'interfície
    boolean esViu();
    String mor();
    String reviu();
}
